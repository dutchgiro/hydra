Given(/^the application id "(.*?)"$/) do |app_id|
  @app_id = app_id
end

When(/^the hydra server is run$/) do
  pending # express the regexp above with the code you wish you had
end

When(/^the application has been saved to hydra server$/) do
  pending # express the regexp above with the code you wish you had
end

Then(/^the response should be success$/) do
  pending # express the regexp above with the code you wish you had
end